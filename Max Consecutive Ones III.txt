class Solution:
    def longestOnes(self, nums: List[int], k: int) -> int:
        ans = i = 0
        for j in range(len(nums)):
            k -= nums[j] == 0
            while k < 0:
                k += nums[i] == 0
                i += 1
            ans = max(ans, j - i + 1)
        return ans